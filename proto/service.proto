syntax = "proto3";
option go_package = "github.com/bakalover/parlia/proto";

service Proxy {
    rpc Apply(Command) returns(Empty);
}

service Replica {
    rpc Apply(Command) returns(Empty);
}

message Command {
    string type = 1;
}

message Empty{}